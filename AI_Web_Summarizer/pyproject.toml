[project]
name = "ai-web-summarizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "parthpetkar24", email = "parth.petkar2020@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "google-generativeai>=0.8.6",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
ai-web-summarizer = "ai_web_summarizer:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
